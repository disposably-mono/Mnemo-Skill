# Mnemo Workspace

Workspace templates and metadata are tracked in git. Real course materials,
Cornell notes, card drafts, and exports are ignored so private study files stay
local.

Copy `courses/_template` for each course and rename the copy. Within each
course, copy `00-module-template` for each module and rename the copy.
